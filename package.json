{
  "name": "the-blueprint",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "E2E=1 vitest run",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "playwright": "^1.47.0"
  }
}
